# 可解释性多模态情感模型说明

本文说明本目录**当前最终模型**的实现，而非原始 DLF 论文的全部训练实验。模型以 DLF 的文本、语音、视觉解耦表征为基础，增加样本自适应融合与完整模态组合归因。主要实现见 `trains/singleTask/model/DLF.py`，当前训练入口是 `run.py`，训练逻辑见 `trains/singleTask/DLF.py`。`adaptive_main=True`、`coalition_aware=True` 才对应下文所述的最终模型。

## 1. 建模原理与任务定义

- 输入为按词对齐的三模态序列：文本 `T`、语音 `A`、视觉 `V`。文本使用 `bert-base-uncased`，语音和视觉使用数据集提供的预提取特征。有效位置掩码区别真实观测与填充或缺失模态；对齐特征中的全零语音／视觉行不被当作有效证据。
- 模型先将每个模态分成**模态特有表征** `s_m` 和经同一个编码器提取的**共享表征** `c_m`。模态特有表征承担门控和内容融合，共享表征补充跨模态信息。重建、特有表征一致性、特有／共享分离及共享表征相似性损失维持这一解耦结构。
- 门控只读取三个模态特有表征的有效位置均值，不读取标签。对于样本 `x`，先得到分数 `g_m(x)`，再将不可用模态屏蔽，计算 `w_m=softmax(g_m/τ)`。`argmax_m w_m` 是 `fusion_main_modality`，表示融合路由偏重的模态。
- 单个融合头输出连续情感强度 `f(x)`（接口名 `output_logit`，实际用于回归）。用附件 2 验证集选出的两个阈值将其划为负向 `0`、中性 `1`、正向 `2`。解释结果中的 `main_modality` 则来自完整模态组合对**本次预测类别**的 Shapley 决策支持；它与门控主模态含义不同，也可能不同。

## 2. 网络结构

1. **输入与投影**：文本 `[batch,3,50]` 包含 BERT 的 token ID、attention mask、segment ID；BERT 输出 768 维。语音与视觉原始特征维度分别为 74、35。三路一维卷积的核宽均为 3，将特征投影到 50 维；长度 50 的对齐序列经无填充卷积成为长度 48。卷积后的掩码随卷积感受野投影。
2. **解耦编码**：三个模态各有一个 50 维 Transformer 特有编码器 `encoder_s_l/a/v`；三个模态共用一个 Transformer 编码器 `encoder_c` 提取共享表示。配置为 2 层、10 个注意力头。每路的特有和共享表示拼接后，经 `1×1` 卷积重建投影特征，并再次编码，以约束特有信息的一致性。
3. **自适应门控**：三个特有序列分别作掩码均值池化，再经过各自的 `LayerNorm + Linear(50,1)` 打分。不可用模态的分数被屏蔽。最终配置使用门控温度 `τ=1.0`；未启用可选的贡献预测门控、跨模态门控补偿或反事实路由分支。
4. **单头融合**：以三个特有池化向量的加权和作查询 `q=Σ_m w_m·pool(s_m)`。一次 10 头动态注意力读取加权后的三路特有 token 与三路共享 token；再把查询、注意力上下文、加权共享池化向量拼接成 150 维，经带残差的两层投影及单个线性输出头得到强度。三路特有辅助回归头与一个共享辅助回归头仅用于训练目标。
5. **模态组合接口**：解释时使用同一套已编码表征和同一个融合头，枚举 `∅、T、A、V、TA、TV、AV、TAV` 八种组合。缺失模态通过有效位置掩码和融合权重处理；空组合强度基线设为 `0`。完整组合分数应与常规预测一致。重新归一化保留模态权重得到“重新路由”分数；固定完整样本权重、将缺失模态权重置零且不重新归一化，得到“固定路由”分数。

## 3. 目标函数

令 `y` 为连续情感标签，`MAE` 为平均绝对误差。代码中自适应模型的完整目标写作：

```text
L_task = MAE(f_TAV, y) + 0.3 MAE(f_shared, y)
         + 0.2 Σ_m MAE(f_specific,m, y)     （只统计可用模态）
L_DLF  = L_task + 0.1 (L_specific + L_recon + 0.1 (L_sim + L_ort))
L_all  = L_DLF + 0.2 MAE(f_S, y) + L_gate
```

- `L_recon` 是各模态投影特征与重建结果的掩码均方误差之和；`L_specific` 是原特有表征与重建后重新编码特有表征的掩码均方误差之和。
- `L_ort` 使用特有与共享表征之间的余弦嵌入损失，促使两类表示分离；`L_sim` 是对共享表征的成对排序约束，实现位于 `trains/singleTask/HingeLoss.py`。
- `S` 先从三模态集合的六个**非空真子集**中为每个样本随机选择，再与实际可用模态取交集；若交集为空，则退回一个可用模态。因此当原样本本身缺少模态时，所得组合也可能包含其全部可用模态。该项让同一融合头学习在部分模态缺失时回归标签，而不是另训七个子模型。
- 当使用常规可学习门控时，`L_gate` 包含基于单模态预测误差的软目标交叉熵、门控均衡项和熵项，默认系数分别为 `0.25`、`2.0`、`0.03`。这些目标只在训练时使用标签，推理门控不读取标签。

**当前最终训练阶段的有效优化范围**：`run.py` 设置 `train_fusion_only=True`，从 `checkpoints/transfer_init/model.pth` 初始化后冻结除 `dynamic_attention`、`adaptive_proj1/2`、`adaptive_out` 外的全部参数。因此，上式虽在训练循环中计算，真正对可训练参数产生梯度的主要是完整样本回归项和系数为 `0.2` 的随机组合回归项；解耦辅助头及门控参数在这个阶段不更新。该初始化检查点是复训前提，本目录没有把它的生成过程作为当前训练入口的一部分。冻结参数不等于禁用训练态随机性：训练循环仍调用模型的 `train()`。

## 4. 可解释性计算与边界

对每个组合 `S` 计算强度 `f(S)`，先以完整预测的类别 `c` 固定解释目标。类别决策边际 `v_c(S)` 定义为：负向时 `lower-f(S)`，正向时 `f(S)-upper`，中性时 `min(f(S)-lower, upper-f(S))`。三模态的精确 Shapley 值为：

```text
φ_m = Σ_{S⊆{T,A,V}\{m}} |S|!(2-|S|)! / 3! · [v_c(S∪{m}) - v_c(S)]
```

`shapley_decision_support` 为重新路由组合的 `φ_m`；`shapley_content_support` 为固定路由组合的同类值；二者之差 `shapley_route_effect` 描述模型内的路由影响。`shapley_score_effect` 则直接分解连续强度，不等同于类别决策支持。`positive_support_share` 将正的决策支持归一化；负值仍应保留在原始有符号支持中。

`main_modality` 选正向决策支持最大的可用模态；若所有支持均不为正，退回到绝对强度 Shapley 效应最大的模态，并标记不具备明确正向支持。只有至少两个模态可用、最大正向支持不小于 `0.02` 且与第二名的差不小于 `0.02` 时，`main_uncertain` 才为 `false`。移除某模态后重新路由得到的类别变化由 `full_modality_class_changed` 记录。附件 4 的局部证据还通过宽度默认为 3 的有效位置遮挡窗口，按原预测类别边际下降排序。

这些数值解释的是**已训练模型在指定遮挡与路由规则下的行为**。门控权重、注意力和 Shapley 值都不能直接称为现实因果效应，`main_modality` 也不是已验证的客观“真实最重要模态”。文本证据可回指提供的原文；音频时间和视频帧因缺少原始特征行到媒体时间的权威映射，仅为候选位置。附件4证据流程以实际解码帧数限制视频范围，全文与音轨逐词匹配率低于 `0.70` 时不输出媒体时间／帧。逐样本 `error_attribution` 标记对齐视觉全零、超过50 token的文本截断及文本—媒体不一致；它们是输入／证据风险，附件4无真实标签时不得直接称为分类错误原因。详细限制见 `docs/attachment4_evidence.md`。

## 5. 训练、校准与关键参数

| 项目 | 当前配置 / 做法 |
| --- | --- |
| 训练数据 | 附件 2 的 `aligned_50.pkl`；`train` 用于训练，`valid` 用于选择检查点与阈值；附件 4 仅用于无标签推理 |
| 初始化与输出 | `checkpoints/transfer_init/model.pth` → `checkpoints/final/model.pth` |
| 随机种子 | `1111`，可由 `run.py --seed` 更改 |
| 批大小 / 梯度累积 | `16` / `10` 个批次后更新；代码累加梯度，未按累积次数再除 |
| 优化器 / 学习率 | Adam；最终融合头学习率 `3e-4` |
| 学习率调度 | `ReduceLROnPlateau`，按验证集 `Loss`，系数 `0.5`、耐心值 `5` |
| 训练时长 / 早停 | 最多 `5` 轮；验证 `Loss` 连续 `2` 轮无改进则停止；保存最优权重 |
| 梯度裁剪 | 按值裁剪到 `±0.6` |
| 随机组合损失 | 权重 `0.2`；每样本每次前向随机采样三模态真子集，再按可用模态裁剪 |
| BERT / 隐层 / 注意力 | `bert-base-uncased`；隐层 `50`；`10` 头；解耦编码器 `2` 层 |
| 主要 dropout | 文本 `0.1`、注意力 `0.5`、输出 `0.5`；其他值见 `config/config.json` |
| 门控温度 | 验证记录为 `1.0` |
| 三分类阈值 | `lower=-0.0543768443`、`upper=0.2913834453`；`< lower` 为负向、`> upper` 为正向，其余为中性 |

在本目录运行以下命令；第二条会重新训练并改写最终权重，训练后须重新校准并核验解释及附件 4 结果：

```powershell
.\.venv\python.exe run.py --check-config
.\.venv\python.exe run.py --seed 1111
.\.venv\python.exe -m utils.validation --gate-temperatures 1.0
.\.venv\python.exe -m utils.verify_coalition_online
.\.venv\python.exe -m utils.verify_coalition_masks
```

现有模型可用 `DLF.from_final_checkpoint()` 同时读取最终权重与 `outputs/validation/valid_thresholds.json`；调用 `model(..., return_explanation=True)` 获得预测与归因。

当前验证集有 728 条样本：MAE `0.55647`、macro-F1 `0.61582`、准确率 `0.63049`。三分类阈值和分类指标来自**同一验证集**，不是独立测试集估计。在 633 条有明确标签误差增量胜者的验证样本上，解释主模态与该离线代理的一致率为 `56.87%`，低于始终选择文本的 `62.09%`；因此不得据此宣称真实模态重要性识别已得到验证。
